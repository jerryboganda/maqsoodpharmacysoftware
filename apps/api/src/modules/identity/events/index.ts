// Blueprint: docs/system-analysis/17-technical-blueprint.md §2.4 (domain events emitted by a
// module). `identity` emits none in Phase 1 -- this file is the placeholder for when it does
// (e.g. `user.created`, `user.deactivated`), kept so every module has the identical shape
// (§3.2 "Identical internal shape for all 17 modules").
export {};
