// Blueprint: §2.4. `settings` will emit a `settings.changed` event once writes exist (bumping
// `settings_version`, §10.2) so the in-process cache described in §10.3 can invalidate.
// Placeholder in Phase 1 -- read-only endpoint, no writes yet.
export {};
